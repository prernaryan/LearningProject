export const baseUrl: string = 'getBaseUrl(SelectedEnvironment)';

export enum KEYBOARD_TYPE {
  NumberPad = 'number-pad',
  EmailAddress = 'email-address',
  Default = 'default',
}

export enum RETURN_KEY_TYPE {
  Done = 'done',
  Next = 'next',
  Enter = 'enter',
  Search = 'search',
  Send = 'send',
}

export const STORAGE_KEY = {};

export enum ModalAnimations {
  FADE_IN = 'fadeIn',
  FADE_OUT = 'fadeOut',
}
