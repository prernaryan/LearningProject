export type useTimerType = {
  counter: number;
};

export type getRemainingTimeType = {
  time: number;
  isValid: boolean;
};

export type useKeyboardType = {
  isKeyboardShown: boolean;
  keyboardHeight: number;
};
